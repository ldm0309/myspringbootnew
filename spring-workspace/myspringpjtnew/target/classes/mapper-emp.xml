<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper-emp">
	<select id="getEmpByFirstNLastName" resultType="myspring.Emp" parameterType="myspring.Emp">
		select last_name 
			, first_name 
			, employee_id 
			, salary
		from employees
		where 1=1
		<choose>
			 <when test="firstName !=null">
			AND first_name = #{firstName} </when>	
			 <when test="lastName != null"> 
			AND last_name = #{lastName}
			 </when>
		</choose>
	</select>
	<select id="zzzz" resultType="myspring.Emp">
		select last_name 
			, first_name 
			, employee_id 
			, salary
		from employees
		where employee_id = #{id}
	</select>
	<select id="getEmpListByDeptNo" parameterType="int" resultType="myspring.Emp">
		select last_name
			,first_name 
			,salary
			,employee_id 
		from employees
		where department_id = #{deptId}
	</select>
</mapper>